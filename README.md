# Arvin Duble Portfolio


## Files

- `index.html` — complete portfolio markup and content
- `style.css` — complete styling, responsive layout, animation, filtering, and touch interaction rules

Open `index.html` directly in a browser, or host this folder with any static web server. Google Fonts are loaded from the external font stylesheet links in the HTML head.
